🔍 Observation Hint:

Check the pod status: `kubectl get pod database-app -n k8squest`

The container may be in `Error` or restarting. Check its current logs:

`kubectl logs database-app -n k8squest`

If it has already restarted, inspect the previous container instance:

`kubectl logs database-app --previous -n k8squest`

What PostgreSQL configuration error do you see?
